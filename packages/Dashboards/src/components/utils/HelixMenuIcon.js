import React from 'react'

const HelixMenuIcon = () => {
    return (
        <IconButton aria-label="settings">
            <MoreVertIcon />
        </IconButton>
    )
}

export default HelixMenuIcon