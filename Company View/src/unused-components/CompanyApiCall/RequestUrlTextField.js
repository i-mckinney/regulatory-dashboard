import React from 'react';
import TextField from '@material-ui/core/TextField';

export default (props) => (
  <form>
    <TextField id='request-input' label='Enter Request URL' variant='outlined' />
  </form>
);
